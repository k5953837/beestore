- @products.each do |product|
  .col-xs-2
    = link_to product_path(product) do
      = render_product_photo(product.photo)
    %h3
      = product.name
    = render "price", product: product
.col-xs-12
  = paginate @products
